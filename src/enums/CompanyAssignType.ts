export enum CompanyAssignType {
  claim ='claim',
  join_request='join_request',
  invites='invites',
  created='created'
  
  }
  

  export enum BulkImportExportType {
    services = "services",
    exhibitors = "exhibitors",
    import = "import",
    export = "export"
  }
    
